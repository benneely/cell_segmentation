# cell_segmentation
Segmenting Cells with Unet and MaskRCNN


```
jupyter notebook --no-browser --NotebookApp.token='' --NotebookApp.disable_check_xsrf=True
```


```
tensorboard --logdir=mrcnn/log_training
```